/* Uncomment to test karma+webpack setup

describe('universal truths', () => {
  // failing test
  it('should fail gracefully', () => {
    expect(1 + 1).toEqual(2);
  });

  // passing test
  it('should pass admirably', () => {
    expect(5).toBeGreaterThan(4);
  });

  // ignored test
  xit('should obey my whims', () => {
    expect(4).toEqual(40);
  });
});

*/